<!--
Name: Guragampreet Singh
Student Number: 8913870
Date/Time Created: 21/02/2025 3:44pm
Description: Ledger.ejs to Show the Ledger of the Inventory
-->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Inventory Ledger</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">Inventory Ledger</h1>
    <a href="/" class="btn btn-secondary mb-3">Back to Inventory</a>
    <table class="table table-bordered table-striped table-hover">
      <thead class="table">
        <tr>
          <th>Ledger ID</th>
          <th>Inventory ID</th>
          <th>Action</th>
          <th>Details</th>
          <th>TimeStamp</th>
        </tr>
      </thead>
      <tbody>
        <% ledgerEntries.forEach(entry => { %>
          <tr>
            <td><%= entry.id %></td>
            <td><%= entry.inventoryId %></td>
            <td><%= entry.action %></td>
            <td><%= entry.details %></td>
            <td><%= new Date(entry.createdAt).toLocaleString() %></td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
